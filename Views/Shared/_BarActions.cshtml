@using Applications.IntegranTI.com.Properties



<table class="table table-striped table-hover">

    <tr>
        <td>
            <input type="submit" name="ButtonAdd" value="@Resources.ButtonAdd" formaction="@Url.Action("Add", "Companies") " class="btn btn-dark" />
        </td>
        <td>
            <input type="submit" name="ButtonView" value="@Resources.ButtonView" formaction="@Url.Action("View", "Companies") " class="btn btn-dark" />
        </td>
        <td>
            <input type="submit" name="ButtonEdit" value="@Resources.ButtonEdit" formaction="@Url.Action("Edit", "Companies") " class="btn btn-dark" />
        </td>
        <td>
            <input type="submit" name="ButtonDelete" value="@Resources.ButtonDelete" formaction="@Url.Action("Delete", "Companies") " class="btn btn-dark" />
        </td>
    </tr>
</table>